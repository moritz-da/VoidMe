package de.hdmstuttgart.voidme.database.di;


public class DummyFactoryTest {

    /*
    //Requires Application context
    List<LocationEntity> foundLocations = new ArrayList<>();

    @Test
    public void dummyFactory_works() {
        String title = "0";
        if (DbManager.voidLocation == null) {
            DbManager.initDb(getTestContext());
        }
        new DummyFactory(10, Objects.requireNonNull(getTestContext()).getResources()).createDummies();
        foundLocations.addAll(DbManager.voidLocation.locationDao().searchLocation("%" + title + "%"));
        Assert.assertNotNull(foundLocations.get(0));
        DbManager.voidLocation.locationDao().deleteAll();
    }*/
}