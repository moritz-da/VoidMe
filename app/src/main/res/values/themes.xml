<resources xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base application theme. -->
    <style name="Theme.VoidMe" parent="Theme.MaterialComponents.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryAlt</item>
        <item name="colorOnPrimary">@color/textDark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary</item>
        <item name="colorSecondaryVariant">@color/backgroundDark</item>
        <item name="colorOnSecondary">@color/textDark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/backgroundNavDark</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/text</item>
        <item name="android:windowBackground">@color/backgroundAlt</item>
        <item name="android:colorBackgroundFloating">@color/backgroundAlt</item>



    </style>
</resources>