<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VoidMe" parent="Theme.MaterialComponents.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryAlt</item>
        <item name="colorOnPrimary">@color/textAltDark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/background</item>
        <item name="colorSecondaryVariant">@color/textAltDark</item>
        <item name="colorOnSecondary">@color/text</item>
        <!-- Android Status bar -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorSurface</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/backgroundNavDark</item>
        <item name="android:windowBackground">@color/backgroundDark</item>
        <item name="android:colorBackgroundFloating">@color/backgroundAltDark</item>
        <item name="colorSurface">@color/backgroundNavDark</item>
        <!-- <item name="android:background">@color/backgroundDark</item> -->
        <item name="android:textColor">@color/textDark</item>

        <item name="android:checkedButton">@color/primary</item>

    </style>
</resources>